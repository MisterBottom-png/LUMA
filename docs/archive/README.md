# Historical records

This directory contains superseded project-package and cleanup records retained for traceability.

- `drive-package/` contains the former root-level package continuity ledgers.
- `repository-cleanup/` contains the pre-upgrade cleanup report.

These files are historical evidence, not current product rules, current project state, or active agent instructions. Current contracts live in `../codex/`.
